var arr = [1, 2, 3, 4];

arr.length; //4

arr.push(10); // add at end

arr.pop(); //delete at end

arr.shift(); // remove first element and return that element

arr.unshift(8); // add element at first
